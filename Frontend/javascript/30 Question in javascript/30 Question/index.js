let x = prompt("what you want to calculate ?");
let y = eval(x);
console.log(y);
// this is the simplest way by which user can calculte there are more certain way for
//  calculate but by taking input the way can be prompt. other can be like by using condition
